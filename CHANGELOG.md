# Changelog

## Unreleased

- added `broker_company_directory` as a shared company directory separate from CRM leads
- added CSV importer for `deal_worker/bases/companies_may.csv`
- added `/broker/company-directory` API and `/broker` UI block to browse imported companies
- added unified `/broker/company-registry` view that stitches directory, CRM, deals, and outreach history per company
